OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=3001
NEXT_PUBLIC_BACKEND_URL=
BACKEND_URL=http://localhost:3001
OPENAI_DISABLED=
# Email Configuration for Contact Form
# Copy this file to .env.local and fill in your email service details

# SMTP Configuration for Hostinger
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=cameron.blake@influitivezone.com
SMTP_PASS="~i5bE*i]+m5P"

# Email Settings
SMTP_FROM=cameron.blake@influitivezone.com
CONTACT_EMAIL=info@influitivezone.com

# Instructions:
# 1. For Hostinger (Current Setup):
#    - Use smtp.hostinger.com as SMTP_HOST
#    - Set SMTP_PORT=587 and SMTP_SECURE=false
#    - Use your full email address for SMTP_USER
#    - Keep the quotes around the password exactly as shown
#    - Make sure there are no extra spaces in the password
#    - CONTACT_EMAIL is where contact form emails will be delivered
#
# 2. For Gmail:
#    - Enable 2-factor authentication
#    - Generate an App Password (not your regular password)
#    - Use your Gmail address for both SMTP_USER and SMTP_FROM
#    - Set SMTP_HOST=smtp.gmail.com and SMTP_PORT=587
#
# 3. For Outlook/Hotmail:
#    - Use smtp-mail.outlook.com as SMTP_HOST
#    - Set SMTP_PORT=587 and SMTP_SECURE=false
#
# 4. For other email providers:
#    - Check their SMTP settings documentation
#    - Adjust SMTP_HOST, SMTP_PORT, and SMTP_SECURE accordingly
