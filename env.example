OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=5000
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
OPENAI_DISABLED=
# Email Configuration for Contact Form
# Copy this file to .env.local and fill in your email service details

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Settings
SMTP_FROM=your-email@gmail.com
CONTACT_EMAIL=contact@yourdomain.com

# Instructions:
# 1. For Gmail:
#    - Enable 2-factor authentication
#    - Generate an App Password (not your regular password)
#    - Use your Gmail address for both SMTP_USER and SMTP_FROM
#    - Set SMTP_HOST=smtp.gmail.com and SMTP_PORT=587
#
# 2. For Outlook/Hotmail:
#    - Use smtp-mail.outlook.com as SMTP_HOST
#    - Set SMTP_PORT=587 and SMTP_SECURE=false
#
# 3. For other email providers:
#    - Check their SMTP settings documentation
#    - Adjust SMTP_HOST, SMTP_PORT, and SMTP_SECURE accordingly
